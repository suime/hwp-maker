# 템플릿 파일 위치

이 디렉토리에는 한글 문서 템플릿 파일만 위치시킵니다.

- 지원 확장자: `.hwp`, `.hwpx`
- 파일을 이 디렉토리에 넣으면 템플릿 패널에서 자동으로 로드됩니다.
- `.hwpx`처럼 ZIP으로 열리는 문서에 `Preview/PrvImage.png`가 있으면 템플릿 버튼에 프리뷰 이미지가 표시됩니다.

문서 변수와 AI 시스템 프롬프트를 담은 YAML 프리셋은 `public/presets`에 둡니다.
